import React from 'react';

const Book = () => (
  <div className="App">
    <h2>Book Lists</h2>
    <form>
      <div>
        <input type="text" />
      </div>
      <button type="submit">Add Book</button>
    </form>
  </div>
);

export default Book;
