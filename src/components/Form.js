import React from 'react';

const Form = () => (
  <div>
    <form>
      <div>
        <input type="text" />
      </div>
      <button type="submit">Add Book</button>
    </form>
  </div>
);

export default Form;
